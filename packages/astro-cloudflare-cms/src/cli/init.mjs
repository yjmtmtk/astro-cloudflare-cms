import { die } from './util.mjs';

/**
 * Stub — real implementation lands in Task 3.
 * @param {Record<string, unknown>} _flags
 */
export async function init(_flags) {
  die('init lands in Task 3');
}
